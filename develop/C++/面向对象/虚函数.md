Q：为什么析构函数要写成虚函数  
A：可以在释放资源时确保正确调用派生类的析构函数  
适用于这样的写法  
Base *derived = new Derived();  
这样析构函数才需要写成虚函数  
如果是  
Derived *derived = new Derived();  
这样的写法，析构函数即使不是虚函数也能正确先释放子类的，再释放基类的
 
Q：构造函数为什么不定义成虚函数  
A：因为虚函数的调用依赖对象类型，在构造函数期间，对象的类型还未确定，并且在构造期间，虚函数表尚未被填充，无法解析